import React from "react"
import ReactDOM from "react-dom"

// Компонент, отображающий график созданный с помощью CanvasJS библиотеки
function Graph() {
  return (
      <div id="chartContainer" />
  )
}

export default Graph
